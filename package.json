{
  "name": "sheet-tools",
  "version": "0.1.0",
  "scripts": {
    "format": "prettier --write --single-quote '**/*.js'",
    "test": "ava test"
  },
  "dependencies": {
    "xlsx": "^0.11.12"
  },
  "devDependencies": {
    "ava": "^0.24.0",
    "fs-extra": "^4.0.2",
    "prettier": "^1.8.2"
  }
}
